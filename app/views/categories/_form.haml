= form_for @category do |f|
  = f.label :name
  = f.text_field :name
  = f.submit 'Submit', class: 'btn btn-secondary', display: 'inline'
  = button_to "Delete", @category, method: :delete, data: { confirm: "Are you sure?" }, class: 'btn btn-danger'
